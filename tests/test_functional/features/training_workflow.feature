Feature: Model training
  I want to be able to train by model when a dataset containing the features and the target is available.
  # Add task names
  Scenario: train the model when training data is available, I launch the training, a model is added to the model registry
     Given
      When
      Then
